import { ChartCard } from "@/components/charts/chart-card";
import { EnterpriseDataTable } from "@/components/data-tables/enterprise-data-table";
import { EmptyState } from "@/components/empty-states/empty-state";
import { FeatureGateNotice } from "@/components/feature-gates/feature-gate-notice";
import { KpiCard } from "@/components/kpi-cards/kpi-card";
export function DashboardPageScaffold({ title, description, gated = false, gateStatus = "licensed_provider_only" }: { title: string; description: string; gated?: boolean; gateStatus?: string }) { return <div className="space-y-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-1 text-slate-600">{description}</p></div><div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm text-slate-600">Freshness: mock data</div></div><div className="rounded-2xl border border-slate-200 bg-white p-4 text-sm text-slate-600">Filter bar placeholder: account, source, provider, confidence, date range, workspace.</div>{gated ? <FeatureGateNotice status={gateStatus} /> : null}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><KpiCard label="Followers" value="1.2M" delta="+4.2%" /><KpiCard label="Engagement rate" value="5.8%" delta="+0.7%" /><KpiCard label="Mentions" value="842" delta="+12%" /><KpiCard label="Provider confidence" value="98%" /></div><ChartCard title={`${title} trend`} /><EnterpriseDataTable title={`${title} events`} /><EmptyState title="No live integration data in Batch 0" /></div>; }
