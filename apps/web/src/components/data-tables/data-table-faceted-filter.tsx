export function DataTableFacetedFilter() {
  return (
    <div className="inline-flex max-w-full flex-wrap items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm text-slate-600" aria-label="Static faceted filter preview">
      <span className="font-semibold text-slate-700">Preview filters only</span>
      <span className="text-xs text-slate-500">No changes are saved in Alpha</span>
    </div>
  );
}
