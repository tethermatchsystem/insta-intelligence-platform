CREATE TABLE IF NOT EXISTS exports (id BIGSERIAL PRIMARY KEY, tenant_id BIGINT NOT NULL REFERENCES tenants(id) ON DELETE CASCADE, export_type TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'queued', object_key TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now());
